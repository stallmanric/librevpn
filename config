# Configuración de la VPN (tiene que ser la misma para cada nodo)
# ===============================================================

# Nombre de la VPN (por default es el nombre de este directorio)
vpn = $(basename $PWD)

# Rango/máscara de toda la VPN. Tiene que englobar los rangos de 
# los nodos individuales
rango_vpn = 255.255.255.0

# Configuración del nodo
# ======================

# La IP de tu nodo en la VPN
# Elegir el último octeto, que no esté ocupado en
# el archivo lista.
ip = cambiar

# La subnet que va a manejar tu nodo. Por default lo dejamos
# sólo.
subnet = /32

# (Opcional)
# La dirección de acceso público del nodo. Lo mejor es cambiarlo
# por un dominio (dinámico o no) que lleve a este nodo
address = $(wget -qO - ifconfig.me/ip)

# Nombre de tu nodo
node = $(hostname)

# Lista de nodos con los que conectarse inicialmente.
# Cada uno de estos nodos debe tener el archivo hosts/<nodo> de
# este nodo
#conexiones=(nodo_a nodo_b etc)
conexiones = (medieval)
