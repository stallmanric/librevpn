# La IP de tu nodo en la VPN
# Elegir el último octeto
ip=192.168.9.
subnet=/32

# (Opcional)
# La dirección de acceso público del nodo
# Cambiar por un dominio que lleve a este nodo
address=$(wget -qO - ifconfig.me/ip)

# Autoconfigurar info
# Nombre de la VPN (lab por default)
vpn=$(basename $PWD)

# Nombre de tu nodo
node=$(hostname)

# La ip de otro nodo al que enlazar
bro=192.168.9.
