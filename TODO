# Cosas para hacer

> No puede hacerlo otro?

* `grep -rHn TODO .`

* lvpn-check. Chequear errores comunes (falta tinc.conf, hosts/minodo,
  hosts/conectara)

* Bash completion

* Publicar los nodos con git-daemon ad-hoc (en un puerto especial)

* Administrar tincd, convertir el log a un socket y poder leer eventos desde ahi

* Usar script host-{up,down} para saber cuales son los nodos disponibles

* Autenticar amigo - F2F - key signing party - agrega mi nodo como amigo (en
  persona) -- keyserver y announce/discover

* Usar miniupnpc/natpmp para pedir un puerto en los nodos portatiles

* Que connectto le mande la llave al otro nodo de alguna forma

vim:set ts=2 sw=2 et syntax=markdown:
