# Adds nodes to connect to

Usage:
lvpn connectto node [host host2 host3...]

Examples:

* List node's peers

    lvpn connectto noanoa

* Add peer nodes to noanoa

    lvpn connectto noanoa ponape medieval
