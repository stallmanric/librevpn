# Exportar los enlaces y nodos a D3

Extrae los nodos y enlaces conocidos del log de tincd y los imprime en JSON
para D3.

El daemon tincd debe iniciarse con *--logfile*.

Uso:

    lvpn d3 > data.json
